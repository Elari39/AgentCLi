# MewCode 项目简历模板

派大星 6月25日修改

按语言分为 Python、Java、Go 三组，每组各三份完整简历，直接复制即可使用。

• **版本 A：AI 辅助实现** ：坦诚使用 AI 编程工具辅助开发，强调架构设计与技术决策能力。括号里的工具换成自己熟悉的，如 Claude Code、Codex、Cursor 等。

• **版本 B：独立实现** ：强调独立完成全部设计与实现

• **版本 C：社招参考** ：包装为公司内部自研提效工具，强调业务价值与团队落地

早期加入课程的同学可以直接参考以下模板来写，但建议把 Mewcode 替换成自己的项目名称，比如 BirdCode、CodeY 等，避免简历撞车。

## Python 版本

### Python 版本 A：AI 辅助实现

#### 项目经历

**Mewcode Coding Agent — 终端 AI 编程助手**

技术栈：Python, MCP, ReAct, Skill, Multi-Agent

**项目介绍** ：轻量级终端 Coding Agent，基于 ReAct 与 Plan Mode 双模式驱动 LLM 自主完成编程任务。交互、引擎、工具、记忆、安全五层分层架构，支持 Anthropic、OpenAI 双协议、MCP 工具扩展、Skill 技能包、跨会话记忆、多 Agent 并行协作。

**个人职责** ：

• 独立负责五层分层架构设计与全部核心技术决策，借助 Claude Code 完成模块编码与调试

• 设计并实现跨会话记忆系统，包括 JSONL 持久化、LLM 自动记忆提取与项目指令文件分级加载

• 负责核心技术方案选型，包括多 LLM 协议适配、上下文两层压缩策略、MCP 工具延迟加载与 Skill 技能体系方案

**技术亮点** ：

• **MCP 工具延迟加载** ：MCP 工具注册时只传名称不传完整 schema，LLM 按需发现后再加载完整定义， **百级工具场景下工具描述 Token 占用减少 85%** ，避免上下文窗口被工具定义挤占

• **多 LLM 协议适配** ：统一 Anthropic、OpenAI 两套不同的流式响应协议为同一套事件接口， **新增 LLM 供应商只需适配一个接口**

• **两层上下文压缩** ：设计两层渐进式上下文压缩策略，自动对齐 Function Calling 的调用配对约束， **支持数小时连续编程会话不丢失上下文**

• **五层权限拦截** ：设计覆盖命令拦截、路径沙箱、规则引擎、权限模式、人工确认的五层权限拦截模型， **任一层拒绝即终止操作，支持 Agent 全自动模式安全执行**

• **自动记忆提取** ：每轮对话结束后异步调 LLM 回顾对话内容，自动将用户偏好、纠正反馈、项目知识、参考信息四类记忆分类持久化到独立文件， **跨会话知识持续累积，同类错误纠正一次后自动规避，新会话自动继承项目上下文无需重复交代**

• **多 Agent 协作** ：将复杂任务拆分给多个 Agent 并行处理，基于 Git Worktree 实现文件级隔离避免编辑冲突，Coordinator Agent 只负责任务拆分与结果汇总不参与编码， **突破单 Agent 上下文窗口瓶颈，大型任务处理效率成倍提升**

### Python 版本 B：独立实现

#### 项目经历

**Mewcode Coding Agent — 终端 AI 编程助手**

技术栈：Python, MCP, ReAct, Skill, Multi-Agent

**项目介绍** ：轻量级终端 Coding Agent，基于 ReAct 与 Plan Mode 双模式驱动 LLM 自主完成编程任务。交互、引擎、工具、记忆、安全五层分层架构，支持 Anthropic、OpenAI 双协议、MCP 工具扩展、Skill 技能包、跨会话记忆、多 Agent 并行协作。

**个人职责** ：

• 独立完成五层分层架构设计与全部模块实现，涵盖交互、引擎、工具、记忆、安全五层的设计与编码调试

• 设计并实现跨会话记忆系统，包括 JSONL 持久化、LLM 自动记忆提取与项目指令文件分级加载

• 负责核心技术选型与方案决策，包括多 LLM 协议适配、上下文两层压缩策略、MCP 工具延迟加载与 Skill 技能体系方案

**技术亮点** ：

• **MCP 工具延迟加载** ：MCP 工具注册时只传名称不传完整 schema，LLM 按需发现后再加载完整定义， **百级工具场景下工具描述 Token 占用减少 85%** ，避免上下文窗口被工具定义挤占

• **多 LLM 协议适配** ：统一 Anthropic、OpenAI 两套不同的流式响应协议为同一套事件接口， **新增 LLM 供应商只需适配一个接口**

• **两层上下文压缩** ：设计两层渐进式上下文压缩策略，自动对齐 Function Calling 的调用配对约束， **支持数小时连续编程会话不丢失上下文**

• **五层权限拦截** ：设计覆盖命令拦截、路径沙箱、规则引擎、权限模式、人工确认的五层权限拦截模型， **任一层拒绝即终止操作，支持 Agent 全自动模式安全执行**

• **自动记忆提取** ：每轮对话结束后异步调 LLM 回顾对话内容，自动将用户偏好、纠正反馈、项目知识、参考信息四类记忆分类持久化到独立文件， **跨会话知识持续累积，同类错误纠正一次后自动规避，新会话自动继承项目上下文无需重复交代**

• **多 Agent 协作** ：将复杂任务拆分给多个 Agent 并行处理，基于 Git Worktree 实现文件级隔离避免编辑冲突，Coordinator Agent 只负责任务拆分与结果汇总不参与编码， **突破单 Agent 上下文窗口瓶颈，大型任务处理效率成倍提升**

### Python 版本 C：社招参考

#### 项目经历

**企业自研 Coding Agent — AI 编程提效平台**

技术栈：Python, Anthropic SDK, OpenAI SDK, MCP, ReAct, Multi-Agent

**项目介绍** ：公司研发团队日常编码中重复性工作多、跨项目上下文切换成本高，为提升工程效率，主导自研终端 AI 编程助手。基于 ReAct 与 Plan Mode 双模式驱动 LLM 自主完成编程任务，五层分层架构支持多模型接入、MCP 工具生态扩展、跨会话记忆和多 Agent 并行协作，已在团队内部推广使用。

**个人职责** ：

• 从 0 到 1 负责整体架构设计与技术选型，确定五层分层架构方案，完成核心模块开发与团队内部推广落地

• 设计并实现多 LLM 供应商接入层，统一 Anthropic、OpenAI 两套流式协议为同一套事件接口，支撑团队按需切换模型

• 负责上下文管理与成本控制方案，设计两层渐进式压缩策略，保障长时间编程会话不中断的同时控制 Token 消耗

**技术亮点** ：

• **MCP 工具延迟加载** ：工具注册时只传名称不传完整 schema，LLM 按需发现后再加载完整定义， **百级工具场景下工具描述 Token 占用减少 85%** ，解决了工具生态膨胀后上下文被挤占的问题

• **多模型统一接入** ：抽象 LLM 交互层统一两套不同的流式响应协议， **新增模型供应商只需实现一个适配接口** ，团队可按成本和能力灵活切换模型

• **两层上下文压缩** ：Layer 1 每轮无条件落盘裁剪零 LLM 调用，Layer 2 在 80% 阈值时触发全量摘要， **单次会话可持续数小时不丢失关键上下文** ，Token 成本较无压缩方案显著降低

• **五层权限拦截** ：覆盖命令拦截、路径沙箱、规则引擎、权限模式、人工确认， **任一层拒绝即终止操作** ，保障 Agent 在全自动模式下安全执行，未出现过越权操作事故

• **自动记忆提取** ：每轮对话结束后异步调 LLM 回顾对话内容，自动将用户偏好、纠正反馈、项目知识、参考信息四类记忆分类持久化到独立文件， **跨会话知识持续累积，同类错误纠正一次后团队全员自动规避，新会话自动继承项目上下文无需重复交代**

• **多 Agent 并行协作** ：基于 Git Worktree 实现文件级隔离，Coordinator Agent 只负责任务拆分与结果汇总不参与编码， **大型重构任务处理效率成倍提升**

## Java 版本

### Java 版本 A：AI 辅助实现

#### 项目经历

**Mewcode Coding Agent — 终端 AI 编程助手**

技术栈：Java 21, MCP, ReAct, Skill, Multi-Agent

**项目介绍** ：轻量级终端 Coding Agent，基于 ReAct 与 Plan Mode 双模式驱动 LLM 自主完成编程任务。交互、引擎、工具、记忆、安全五层分层架构，支持 Anthropic、OpenAI 双协议、MCP 工具扩展、Skill 技能包、跨会话记忆、多 Agent 并行协作。

**个人职责** ：

• 独立负责五层分层架构设计与全部核心技术决策，借助 Claude Code 完成模块编码与调试

• 设计并实现跨会话记忆系统，包括 JSONL 持久化、LLM 自动记忆提取与项目指令文件分级加载

• 负责核心技术方案选型，包括多 LLM 协议适配、上下文两层压缩策略、MCP 工具延迟加载与 Skill 技能体系方案

**技术亮点** ：

• **MCP 工具延迟加载** ：MCP 工具注册时只传名称不传完整 schema，LLM 按需发现后再加载完整定义， **百级工具场景下工具描述 Token 占用减少 85%** ，避免上下文窗口被工具定义挤占

• **多 LLM 协议适配** ：统一 Anthropic、OpenAI 两套不同的流式响应协议为同一套事件接口， **新增 LLM 供应商只需适配一个接口**

• **两层上下文压缩** ：设计两层渐进式上下文压缩策略，自动对齐 Function Calling 的调用配对约束， **支持数小时连续编程会话不丢失上下文**

• **五层权限拦截** ：设计覆盖命令拦截、路径沙箱、规则引擎、权限模式、人工确认的五层权限拦截模型， **任一层拒绝即终止操作，支持 Agent 全自动模式安全执行**

• **自动记忆提取** ：每轮对话结束后异步调 LLM 回顾对话内容，自动将用户偏好、纠正反馈、项目知识、参考信息四类记忆分类持久化到独立文件， **跨会话知识持续累积，同类错误纠正一次后自动规避，新会话自动继承项目上下文无需重复交代**

• **多 Agent 协作** ：将复杂任务拆分给多个 Agent 并行处理，基于 Git Worktree 实现文件级隔离避免编辑冲突，Coordinator Agent 只负责任务拆分与结果汇总不参与编码， **突破单 Agent 上下文窗口瓶颈，大型任务处理效率成倍提升**

### Java 版本 B：独立实现

#### 项目经历

**Mewcode Coding Agent — 终端 AI 编程助手**

技术栈：Java 21, MCP, ReAct, Skill, Multi-Agent

**项目介绍** ：轻量级终端 Coding Agent，基于 ReAct 与 Plan Mode 双模式驱动 LLM 自主完成编程任务。交互、引擎、工具、记忆、安全五层分层架构，支持 Anthropic、OpenAI 双协议、MCP 工具扩展、Skill 技能包、跨会话记忆、多 Agent 并行协作。

**个人职责** ：

• 独立完成五层分层架构设计与全部模块实现，涵盖交互、引擎、工具、记忆、安全五层的设计与编码调试

• 设计并实现跨会话记忆系统，包括 JSONL 持久化、LLM 自动记忆提取与项目指令文件分级加载

• 负责核心技术选型与方案决策，包括多 LLM 协议适配、上下文两层压缩策略、MCP 工具延迟加载与 Skill 技能体系方案

**技术亮点** ：

• **MCP 工具延迟加载** ：MCP 工具注册时只传名称不传完整 schema，LLM 按需发现后再加载完整定义， **百级工具场景下工具描述 Token 占用减少 85%** ，避免上下文窗口被工具定义挤占

• **多 LLM 协议适配** ：统一 Anthropic、OpenAI 两套不同的流式响应协议为同一套事件接口， **新增 LLM 供应商只需适配一个接口**

• **两层上下文压缩** ：设计两层渐进式上下文压缩策略，自动对齐 Function Calling 的调用配对约束， **支持数小时连续编程会话不丢失上下文**

• **五层权限拦截** ：设计覆盖命令拦截、路径沙箱、规则引擎、权限模式、人工确认的五层权限拦截模型， **任一层拒绝即终止操作，支持 Agent 全自动模式安全执行**

• **自动记忆提取** ：每轮对话结束后异步调 LLM 回顾对话内容，自动将用户偏好、纠正反馈、项目知识、参考信息四类记忆分类持久化到独立文件， **跨会话知识持续累积，同类错误纠正一次后自动规避，新会话自动继承项目上下文无需重复交代**

• **多 Agent 协作** ：将复杂任务拆分给多个 Agent 并行处理，基于 Git Worktree 实现文件级隔离避免编辑冲突，Coordinator Agent 只负责任务拆分与结果汇总不参与编码， **突破单 Agent 上下文窗口瓶颈，大型任务处理效率成倍提升**

### Java 版本 C：社招参考

#### 项目经历

**企业自研 Coding Agent — AI 编程提效平台**

技术栈：Java 21, Anthropic SDK, OpenAI SDK, MCP, ReAct, Multi-Agent

**项目介绍** ：公司研发团队日常编码中重复性工作多、跨项目上下文切换成本高，为提升工程效率，主导自研终端 AI 编程助手。基于 ReAct 与 Plan Mode 双模式驱动 LLM 自主完成编程任务，五层分层架构支持多模型接入、MCP 工具生态扩展、跨会话记忆和多 Agent 并行协作，已在团队内部推广使用。

**个人职责** ：

• 从 0 到 1 负责整体架构设计与技术选型，确定五层分层架构方案，完成核心模块开发与团队内部推广落地

• 设计并实现多 LLM 供应商接入层，统一 Anthropic、OpenAI 两套流式协议为同一套事件接口，支撑团队按需切换模型

• 负责上下文管理与成本控制方案，设计两层渐进式压缩策略，保障长时间编程会话不中断的同时控制 Token 消耗

**技术亮点** ：

• **MCP 工具延迟加载** ：工具注册时只传名称不传完整 schema，LLM 按需发现后再加载完整定义， **百级工具场景下工具描述 Token 占用减少 85%** ，解决了工具生态膨胀后上下文被挤占的问题

• **多模型统一接入** ：抽象 LLM 交互层统一两套不同的流式响应协议， **新增模型供应商只需实现一个适配接口** ，团队可按成本和能力灵活切换模型

• **两层上下文压缩** ：Layer 1 每轮无条件落盘裁剪零 LLM 调用，Layer 2 在 80% 阈值时触发全量摘要， **单次会话可持续数小时不丢失关键上下文** ，Token 成本较无压缩方案显著降低

• **五层权限拦截** ：覆盖命令拦截、路径沙箱、规则引擎、权限模式、人工确认， **任一层拒绝即终止操作** ，保障 Agent 在全自动模式下安全执行，未出现过越权操作事故

• **自动记忆提取** ：每轮对话结束后异步调 LLM 回顾对话内容，自动将用户偏好、纠正反馈、项目知识、参考信息四类记忆分类持久化到独立文件， **跨会话知识持续累积，同类错误纠正一次后团队全员自动规避，新会话自动继承项目上下文无需重复交代**

• **多 Agent 并行协作** ：基于 Git Worktree 实现文件级隔离，Coordinator Agent 只负责任务拆分与结果汇总不参与编码， **大型重构任务处理效率成倍提升**

## Go 版本

### Go 版本 A：AI 辅助实现

#### 项目经历

**Mewcode Coding Agent — 终端 AI 编程助手**

技术栈：Go, MCP, ReAct, Skill, Multi-Agent

**项目介绍** ：轻量级终端 Coding Agent，基于 ReAct 与 Plan Mode 双模式驱动 LLM 自主完成编程任务。交互、引擎、工具、记忆、安全五层分层架构，支持 Anthropic、OpenAI 双协议、MCP 工具扩展、Skill 技能包、跨会话记忆、多 Agent 并行协作。

**个人职责** ：

• 独立负责五层分层架构设计与全部核心技术决策，借助 Claude Code 完成模块编码与调试

• 设计并实现跨会话记忆系统，包括 JSONL 持久化、LLM 自动记忆提取与项目指令文件分级加载

• 负责核心技术方案选型，包括多 LLM 协议适配、上下文两层压缩策略、MCP 工具延迟加载与 Skill 技能体系方案

**技术亮点** ：

• **MCP 工具延迟加载** ：MCP 工具注册时只传名称不传完整 schema，LLM 按需发现后再加载完整定义， **百级工具场景下工具描述 Token 占用减少 85%** ，避免上下文窗口被工具定义挤占

• **多 LLM 协议适配** ：统一 Anthropic、OpenAI 两套不同的流式响应协议为同一套事件接口， **新增 LLM 供应商只需适配一个接口**

• **两层上下文压缩** ：设计两层渐进式上下文压缩策略，自动对齐 Function Calling 的调用配对约束， **支持数小时连续编程会话不丢失上下文**

• **五层权限拦截** ：设计覆盖命令拦截、路径沙箱、规则引擎、权限模式、人工确认的五层权限拦截模型， **任一层拒绝即终止操作，支持 Agent 全自动模式安全执行**

• **自动记忆提取** ：每轮对话结束后异步调 LLM 回顾对话内容，自动将用户偏好、纠正反馈、项目知识、参考信息四类记忆分类持久化到独立文件， **跨会话知识持续累积，同类错误纠正一次后自动规避，新会话自动继承项目上下文无需重复交代**

• **多 Agent 协作** ：将复杂任务拆分给多个 Agent 并行处理，基于 Git Worktree 实现文件级隔离避免编辑冲突，Coordinator Agent 只负责任务拆分与结果汇总不参与编码， **突破单 Agent 上下文窗口瓶颈，大型任务处理效率成倍提升**

### Go 版本 B：独立实现

#### 项目经历

**Mewcode Coding Agent — 终端 AI 编程助手**

技术栈：Go, MCP, ReAct, Skill, Multi-Agent

**项目介绍** ：轻量级终端 Coding Agent，基于 ReAct 与 Plan Mode 双模式驱动 LLM 自主完成编程任务。交互、引擎、工具、记忆、安全五层分层架构，支持 Anthropic、OpenAI 双协议、MCP 工具扩展、Skill 技能包、跨会话记忆、多 Agent 并行协作。

**个人职责** ：

• 独立完成五层分层架构设计与全部模块实现，涵盖交互、引擎、工具、记忆、安全五层的设计与编码调试

• 设计并实现跨会话记忆系统，包括 JSONL 持久化、LLM 自动记忆提取与项目指令文件分级加载

• 负责核心技术选型与方案决策，包括多 LLM 协议适配、上下文两层压缩策略、MCP 工具延迟加载与 Skill 技能体系方案

**技术亮点** ：

• **MCP 工具延迟加载** ：MCP 工具注册时只传名称不传完整 schema，LLM 按需发现后再加载完整定义， **百级工具场景下工具描述 Token 占用减少 85%** ，避免上下文窗口被工具定义挤占

• **多 LLM 协议适配** ：统一 Anthropic、OpenAI 两套不同的流式响应协议为同一套事件接口， **新增 LLM 供应商只需适配一个接口**

• **两层上下文压缩** ：设计两层渐进式上下文压缩策略，自动对齐 Function Calling 的调用配对约束， **支持数小时连续编程会话不丢失上下文**

• **五层权限拦截** ：设计覆盖命令拦截、路径沙箱、规则引擎、权限模式、人工确认的五层权限拦截模型， **任一层拒绝即终止操作，支持 Agent 全自动模式安全执行**

• **自动记忆提取** ：每轮对话结束后异步调 LLM 回顾对话内容，自动将用户偏好、纠正反馈、项目知识、参考信息四类记忆分类持久化到独立文件， **跨会话知识持续累积，同类错误纠正一次后自动规避，新会话自动继承项目上下文无需重复交代**

• **多 Agent 协作** ：将复杂任务拆分给多个 Agent 并行处理，基于 Git Worktree 实现文件级隔离避免编辑冲突，Coordinator Agent 只负责任务拆分与结果汇总不参与编码， **突破单 Agent 上下文窗口瓶颈，大型任务处理效率成倍提升**

### Go 版本 C：社招参考

#### 项目经历

**企业自研 Coding Agent — AI 编程提效平台**

技术栈：Go, Anthropic SDK, OpenAI SDK, MCP, ReAct, Multi-Agent

**项目介绍** ：公司研发团队日常编码中重复性工作多、跨项目上下文切换成本高，为提升工程效率，主导自研终端 AI 编程助手。基于 ReAct 与 Plan Mode 双模式驱动 LLM 自主完成编程任务，五层分层架构支持多模型接入、MCP 工具生态扩展、跨会话记忆和多 Agent 并行协作，已在团队内部推广使用。

**个人职责** ：

• 从 0 到 1 负责整体架构设计与技术选型，确定五层分层架构方案，完成核心模块开发与团队内部推广落地

• 设计并实现多 LLM 供应商接入层，统一 Anthropic、OpenAI 两套流式协议为同一套事件接口，支撑团队按需切换模型

• 负责上下文管理与成本控制方案，设计两层渐进式压缩策略，保障长时间编程会话不中断的同时控制 Token 消耗

**技术亮点** ：

• **MCP 工具延迟加载** ：工具注册时只传名称不传完整 schema，LLM 按需发现后再加载完整定义， **百级工具场景下工具描述 Token 占用减少 85%** ，解决了工具生态膨胀后上下文被挤占的问题

• **多模型统一接入** ：抽象 LLM 交互层统一两套不同的流式响应协议， **新增模型供应商只需实现一个适配接口** ，团队可按成本和能力灵活切换模型

• **两层上下文压缩** ：Layer 1 每轮无条件落盘裁剪零 LLM 调用，Layer 2 在 80% 阈值时触发全量摘要， **单次会话可持续数小时不丢失关键上下文** ，Token 成本较无压缩方案显著降低

• **五层权限拦截** ：覆盖命令拦截、路径沙箱、规则引擎、权限模式、人工确认， **任一层拒绝即终止操作** ，保障 Agent 在全自动模式下安全执行，未出现过越权操作事故

• **自动记忆提取** ：每轮对话结束后异步调 LLM 回顾对话内容，自动将用户偏好、纠正反馈、项目知识、参考信息四类记忆分类持久化到独立文件， **跨会话知识持续累积，同类错误纠正一次后团队全员自动规避，新会话自动继承项目上下文无需重复交代**

• **多 Agent 并行协作** ：基于 Git Worktree 实现文件级隔离，Coordinator Agent 只负责任务拆分与结果汇总不参与编码， **大型重构任务处理效率成倍提升**
